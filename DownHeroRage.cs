using System.Collections;
using System.Collections.Generic;
using System.Threading.Tasks;
using UnityEngine;

public class DownHeroRage : MonoBehaviour
{
    /*private HeroMovement hm;
    private Rigidbody hero;
    private bool temp;
    void Awake()
    {
        hm = GameObject.Find("Hero").GetComponent<HeroMovement>();
        hero = GameObject.Find("Hero").GetComponent<Rigidbody>();
    }
    // Start is called before the first frame update
    private async void OnTriggerEnter(Collider other)
    {
        if (other.gameObject.CompareTag("Player"))
        {
        }
    }

    private async void Update()
    {
        if (hm.IsAgresion)
        {            
            await Task.Delay(300);
            hero.AddForce(new Vector3(0, 0, 0), ForceMode.VelocityChange);
  
        }
    }*/
}
